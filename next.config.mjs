/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'cdn.pixabay.com',
      'images.unsplash.com',
      'picsum.photos'
    ],
  },
};

export default nextConfig;

